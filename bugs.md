- Any math operations after a function call in a statement are ignored and the statement is parsed entirely like a call

- Bindings shouldn't have a nested assignment

- Top-level parsing doesn't work at all
