//COMMENT
/*
MULTI LINE COMMENT
*/
//cimport stdio.h;
import point;

enum A {
    Q, X, Z
}

func incr(a $i32) void {
    $a = $a + 1;
}

func main(argc i32, argv $$u8) i32 {
    c := new point;
    c.x = 5;
    c.y = 6;
    q := A::Q;
    printf("%d\n", q);
	printf("My command line args:\n");
    for a := 0; a < argc; a = a + 1 {
        b := argv[a];
        printf("%s\n", b);
    }
	return 0;
}
